workspace:
  base: /test
  path: webtools-geocoding-provider

services:
  web:
    image: fpfis/httpd-php-ci:${PHP_VERSION}
    environment:
      - DOCUMENT_ROOT=/test/webtools-geocoding-provider

pipeline:
  composer-install:
    group: prepare
    image: fpfis/httpd-php-ci:${PHP_VERSION}
    volumes:
      - /cache:/cache
    commands:
      - composer install

# The code review tool is not yet stable for pure PHP 7 libraries. It throws a
# large number of false positives. Re-enable this once the tool works properly.
#  grumphp:
#    group: test
#    image: fpfis/httpd-php-ci:${PHP_VERSION}
#    commands:
#      - ./vendor/bin/grumphp run

  phpunit:
    group: test
    image: fpfis/httpd-php-ci:${PHP_VERSION}
    commands:
      - ./vendor/bin/phpunit

matrix:
  PHP_VERSION:
    - 7.1
    - 7.2
    - 7.3
    - 7.4
